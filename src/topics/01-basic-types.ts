const name = 'Sergio';
let hpPoints: number | string = 95;
let hpPoints1: number | 'FULL' = 95;
const isAlive : boolean = true;

 hpPoints = '123'
 hpPoints1 = 'FULL'

 console.log({
    name, hpPoints, isAlive
 })

export{}